# test_debug.py
def calculate_average(numbers):
    total = 0
    for num in numbers:
        total += num
    return total / len(numbers)

# 测试数据
data = [10, 20, 30, 40]
print("平均值:", calculate_average(data))